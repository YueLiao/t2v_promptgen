# t2v_promptgen runtime dependencies
# Last updated: 2026-05-20 (PR-1 of prompt-rewrite feature)

# Web server
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9   # file uploads (R0 rewrite)

# Data validation
pydantic>=2.6

# LLM clients
requests>=2.31            # used by openai-compat client

# Tag library + config
pyyaml>=6.0

# File parsing (R0 rewrite)
openpyxl>=3.1             # .xlsx
chardet>=5.2              # optional encoding fallback

# Dev / test
pytest>=8.0
pytest-cov>=5.0
